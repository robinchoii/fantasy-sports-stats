const rootReducer = (state=[], action) => {
    switch(action.type) {
        case "GET_WIDE_RECEIVERS":
            return state
        default:
            return state;
    }
}

export default rootReducer
